# Release: 18 September 2026

## Web and backend

- Production: https://www.vlystudios.com
- Supabase: `ahtynqcogyqhcrvqdsmi` (`arcade-score-app`), restored from its paused state and healthy.
- Migration: `20260918152047_project_review_security`.
- Edge Functions: `password-login` v1, `moderate-image` v15, `delete-account` v12. These handlers validate credentials themselves.
- Initial Vercel deployment: `dpl_6Rae3ZWcatuvXtDUC7fm9R3gzhdd`, built with production secrets, verified before promotion. Framework: Expo SDK 55 static web export and Node API functions; build took about 185 seconds.

The production database backup is retained locally in the ignored `.review-build/production-before-review.dump`. The public schema snapshot contains no data and is checked in under `supabase/baselines/`; the private backup is excluded from Git and Vercel uploads.

## Verification

- Full production backup restored into an isolated local PostgreSQL 17 instance. The complete migration applied successfully; authenticated feed/rankings and service-only quota/recap/media/deletion SQL smoke checks passed. Test changes rolled back and the local container had no network access.
- 43 regression tests and TypeScript passed after the migration compatibility fix. ESLint passed with 0 errors and 104 warnings.
- Live migration history and function grants verified. Anonymous identity lookup, karaoke advancement and service quotas are denied; both private media buckets remain private.
- Live password-login CORS preflight returned 204; invalid credentials returned 401. Image moderation, deletion, registration and AI endpoints rejected unauthenticated calls with 401.
- Production desktop login rendered without browser runtime errors.
- No historical registrations, payment events or push tokens required data reconciliation.

Real provider payment completion, image moderation and notifications still require authorized staging accounts/device testing. No real payment, push send or account deletion was performed.

## Native release

Production EAS client environment variables and the existing Sentry upload secret are configured. Android build `5272040e-9deb-41da-a4c5-39941a9b7859` (version code 5) is queued; Expo reports elevated Android build/submission queue times. EAS has an Android keystore, but no Google Play submission service account or FCM v1 credentials. iOS build setup is blocked because signing credentials are missing from EAS. Store publication requires those credentials to be configured.

The native runtime now uses a fingerprint policy and updated native dependencies. Older installed binaries need a new store build; publishing an OTA update alone cannot update them.
