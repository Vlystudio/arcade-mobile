// Native stub — react-native-view-shot would be needed for native capture (requires EAS build).
// On iOS/Android the device's built-in screenshot shortcut works fine.
export function ScreenshotButton() {
  return null;
}
